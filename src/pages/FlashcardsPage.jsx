import { useState } from 'react';

export default function FlashcardsPage() {
  const [activeSet, setActiveSet] = useState(null);

  return <div className="page">{/* твой код без изменений */}</div>;
}
